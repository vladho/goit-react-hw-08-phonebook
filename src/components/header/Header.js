import React from "react";
import NavigationLinks from "../Navigation/NavigationLinks";

const Header = () => {
  return (
    <header>
      <NavigationLinks />
    </header>
  );
};

export default Header;
